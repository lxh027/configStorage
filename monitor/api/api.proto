syntax = "proto3";

option go_package = "lxh027/configStorage/monitor/api";
package monitor;

service TestConnection {
  rpc Test(Ping) returns (Pong);
}

message Ping {
  string ping = 1;
}

message Pong {
  string pong = 1;
}

